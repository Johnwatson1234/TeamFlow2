# TeamFlow 项目开发文档

## 1. 文档目标

本文档用于作为 TeamFlow 项目的**唯一实现级开发说明**之一，配合 `设计图` 目录下的全部页面设计稿，指导后续 Codex 或开发者对整个项目进行完整实现、功能补全与前端界面复刻。

本文档的目标有 4 个：

1. 统一产品范围：明确 TeamFlow 要实现哪些模块、页面、交互与数据流；
2. 统一视觉标准：明确前端必须严格遵循当前已有配色、布局、卡片风格、间距、状态色与组件语言；
3. 统一技术实现：明确前后端目录结构、核心模型、接口、状态管理与页面路由；
4. 统一验收标准：确保后续实现不仅“功能能跑”，还要“页面像设计图、逻辑完整、模块协同正确”。

> 重要原则：后续实现必须以“设计图视觉效果 + 本文档业务说明 + 当前仓库已存在实现”为共同基线，最终做出一个**功能齐全、视觉统一、交互闭环**的 TeamFlow 系统。

---

## 2. 最高约束与优先级

### 2.1 最高约束

1. **当前已有 UI 配色一律不改。**
2. 所有后续新增页面、弹窗、表格、图表、侧边栏、卡片、按钮，都必须沿用当前系统既有的配色风格。
3. 必须尽可能复刻 `设计图` 目录中的页面效果，做到布局、层级、视觉氛围、信息密度一致。
4. 功能必须完整，不允许只做静态壳子；所有关键页面都要与真实后端数据联动。

### 2.2 实现优先级

当出现冲突时，执行优先级如下：

1. 本轮用户指令；
2. 本文档中的业务和实现规则；
3. `设计图` 目录中的视觉与布局要求；
4. 当前项目中已经存在的前后端实现；
5. `doc/开发文档.md`、`通信方案.md`、`要求.md`、`报告.md` 等辅助文档。

### 2.3 设计图适用原则

- `设计图` 中的图片是**目标效果图**，后续实现要尽量达到同等信息结构和视觉效果；
- 若当前代码中的界面与设计图不一致，以设计图为目标进行收敛；
- 若设计图未覆盖某个细节，则参考当前项目已实现的组件样式、变量体系和交互语言；
- 若某个页面设计图只展示主界面，没有展示空状态、加载状态、报错状态，则这些状态必须按同一视觉体系补齐。

---

## 3. 项目定位

### 3.1 项目名称

- 中文：小组协作过程管理软件
- 产品名：`TeamFlow`

### 3.2 产品定位

TeamFlow 是一个面向大学课程设计场景的**AI 驱动协作过程审计与智能项目管理平台**。

系统不是单纯的任务管理工具，也不是单纯的聊天工具，而是一个以“项目空间”为核心，将以下能力统一起来的平台：

- 项目空间管理
- 成员管理与邀请
- 任务分配、任务看板、任务活动时间线
- 即时消息与任务讨论联动
- 协作文档与版本留痕
- 文件共享与资料评审
- Git 提交记录与冲突风险分析
- 协作图谱、贡献审计、风险预警
- AI 项目经理与自动任务拆解
- 通知中心、待办中心、邀请处理中心

### 3.3 目标用户

1. 课程设计小组成员
2. 项目组长 / Leader
3. 指导教师 / 审计角色

---

## 4. 代码与技术基线

## 4.1 前端技术栈

- `Vue 3`
- `TypeScript`
- `Vite`
- `Pinia`
- `Vue Router`
- `Axios`
- `Element Plus`
- `ECharts`
- `@antv/g6`
- `dayjs`

## 4.2 后端技术栈

- `FastAPI`
- `SQLAlchemy`
- `Pydantic`
- `JWT / PyJWT`
- `Passlib`
- `SQLite`（当前默认）
- `httpx`（AI 调用）

## 4.3 当前主要目录结构

```text
backend/
  app/
    api/
    core/
    models/
    schemas/
frontend/
  src/
    api/
    layouts/
    router/
    stores/
    views/
设计图/
开发文档.md
通信方案.md
```

---

## 5. 设计资产清单（必须落地）

后续实现时，以下设计图均视为必须被落实的目标页面：

1. `设计图/登录页.png`
2. `设计图/项目列表页.png`
3. `设计图/项目仪表盘页.png`
4. `设计图/任务管理页.png`
5. `设计图/文档协作页.png`
6. `设计图/AI助手页.png`
7. `设计图/协作图谱贡献分析页.png`
8. `设计图/项目设置成员邀请页.png`
9. `设计图/协作提醒中心页.png`
10. `设计图/即时消息任务联动页.png`
11. `设计图/代码提交冲突风险页.png`
12. `设计图/文件共享资料评审页.png`

> 注意：虽然当前前端路由中部分页面已存在实现，但后续开发的目标不是“保留旧样式”，而是“以这些图为视觉标准进行统一收敛”。

---

## 6. 视觉系统规范（必须严格遵守）

## 6.1 当前全局色板（不得改变）

基于 `frontend/src/style.css`，当前项目已定义以下核心视觉变量：

- 主色：`--primary: #4F46E5`
- 主色浅层：`--primary-light: #6366f1`
- 主色深层：`--primary-dark: #3730a3`
- 青色辅助：`--cyan: #06B6D4`
- 成功色：`--success: #22C55E`
- 警告色：`--warning: #F59E0B`
- 危险色：`--danger: #EF4444`
- AI 紫色：`--ai: #8B5CF6`
- 页面背景：`--bg: #F8FAFC`
- 深浅卡片背景：`--bg-dark: #F1F5F9`
- 卡片白底：`--card: #FFFFFF`
- 边框：`--border: #E2E8F0`
- 主要文字：`--text: #0F172A`
- 次级文字：`--text-muted: #64748B`
- 弱文字：`--text-light: #94A3B8`
- 侧边栏背景：`--sidebar-bg: #1E1B4B`
- 侧边栏文字：`--sidebar-text: #C7D2FE`

## 6.2 圆角与阴影

- 卡片圆角：`10px`
- 小圆角：`6px`
- 大卡片圆角：`16px`
- 卡片阴影：柔和浅阴影，不可使用强烈投影
- Hover：允许轻微上浮（2~4px）与阴影增强

## 6.3 页面基调

必须统一为以下风格：

- 暗色左侧导航 + 亮色主内容区
- 大面积留白 + 中高信息密度
- 卡片式仪表盘 + 统计卡 + 时间线 + 表格/列表混排
- 低饱和蓝紫科技感，而不是高饱和彩虹配色
- 所有图表、标签、状态点保持专业 SaaS 风格

## 6.4 不允许的视觉偏离

1. 不允许改成新的主色系（如绿色主色、红色主色、橙色主色）；
2. 不允许引入完全不同的 UI 框架风格覆盖当前设计语言；
3. 不允许做成移动端优先风格；
4. 不允许使用过重边框、过强渐变、霓虹赛博风；
5. 不允许让不同页面像来自不同产品。

---

## 7. 路由与页面清单

当前路由基线如下：

### 公共页

- `/login` 登录页
- `/register` 注册页
- `/projects` 项目列表页

### 项目页

- `/projects/:id` 项目仪表盘
- `/projects/:id/tasks` 任务管理页
- `/projects/:id/messages` 消息与讨论页
- `/projects/:id/documents` 文档协作页
- `/projects/:id/documents/:docId` 文档编辑页
- `/projects/:id/files` 文件共享页
- `/projects/:id/graph` 协作图谱页
- `/projects/:id/contribution` 贡献审计页
- `/projects/:id/risk` 风险雷达页
- `/projects/:id/git` Git 协作 / 代码提交页
- `/projects/:id/ai` AI 项目经理页
- `/projects/:id/settings` 项目设置页

### 全局侧边栏附属交互

- 协作提醒抽屉（从侧边栏打开）
- 我的待办、通知、邀请处理

---

## 8. 页面级详细实现规范

以下页面必须逐页落地。每个页面都必须同时满足：

- 视觉与设计图一致；
- 数据来源真实；
- 状态完整（空、加载、错误、成功）；
- 交互闭环完整；
- 与其他模块联动。

## 8.1 登录页

参考：`设计图/登录页.png`

### 功能要求

- 用户名登录
- 密码登录
- 登录成功后进入项目列表页
- 登录失败展示错误提示
- 提供跳转注册入口

### 视觉要求

- 必须保持现有登录页的大屏、高级感和品牌感
- 左右分栏或视觉主 KV 风格必须与设计图一致
- 文案中必须体现：协作图谱、AI 项目经理、贡献审计、风险雷达

---

## 8.2 项目列表页

参考：`设计图/项目列表页.png`

### 功能要求

- 展示当前用户加入的项目列表
- 支持创建项目
- 支持展示：项目名、简介、课程名、创建时间、截止日期、状态
- 页面顶部需展示：待处理邀请、我的待办、最新通知三个工作台模块
- 用户可从此页接受或拒绝邀请
- 点击项目卡进入项目仪表盘

### 必须实现的数据模块

- 项目列表
- 我的邀请列表
- 我的待办任务
- 最新通知

### 交互要求

- 创建项目成功后自动跳转到该项目
- 接受邀请后应加入项目并刷新项目列表
- 通知点击后应可跳转对应项目或任务页

---

## 8.3 项目仪表盘页

参考：`设计图/项目仪表盘页.png`

### 功能要求

首页必须承担“项目全局态势看板”作用。

### 必须实现的统计卡

- 任务完成率
- 进行中任务
- 延期任务
- 项目成员
- 协作文档
- 协作事件
- 待处理邀请
- 我的待办
- 未读提醒

### 必须实现的内容区块

1. 项目心电图 / 协作活跃度趋势图
2. 最新协作事件列表
3. 风险预警卡片
4. 成员贡献排名
5. 项目健康雷达
6. 最近任务活动

### 交互要求

- `扫描风险` 按钮触发后端风险扫描
- `刷新贡献分` 按钮触发贡献重算
- 点击详情链接跳转到图谱 / 风险 / 任务 / 贡献页面

### 数据来源

- `/api/projects/{id}/dashboard`
- `/api/projects/{id}/risk-alerts`
- `/api/projects/{id}/contribution`

---

## 8.4 任务管理页

参考：`设计图/任务管理页.png`

### 功能定位

任务页是项目推进核心页面，必须兼顾看板、筛选、任务详情、活动追踪、讨论联动。

### 布局要求

推荐目标结构：

- 顶部：标题、筛选器、批量操作、新建任务
- 中部：Kanban 多列看板
- 右侧或抽屉：任务详情
- 下部：里程碑进度 / 统计卡

### 必须实现的状态列

- 待处理 `TODO`
- 进行中 `IN_PROGRESS`
- 阻塞中 `BLOCKED`
- 待评审 `REVIEW`
- 已完成 `DONE`

### 每个任务卡必须展示

- 标题
- 优先级
- 当前响应状态（未分配 / 待接收 / 处理中 / 阻塞中 / 待验收 / 已完成）
- 负责人
- 截止时间
- 进度条
- 简要描述

### 任务详情必须展示

- 基本信息
- 任务描述
- 负责人
- 参与人 / 跟随者（可扩展）
- 优先级
- 进度
- 截止时间
- 讨论区
- 活动记录时间线
- 关联资源（后续可扩）

### 必须实现的核心交互

- 新建任务
- 拖拽切换状态
- 编辑任务
- 删除任务（归档）
- 指派负责人
- 负责人自己“接收并开始处理”
- 更新进度
- 发布任务讨论
- 拉取任务活动时间线

### 后端联动要求

- 创建任务时写入 `CollaborationEvent` 和 `TaskActivity`
- 分配任务时写入通知
- 更新状态时写入活动与通知
- 任务讨论消息要写入消息表与任务活动表

---

## 8.5 消息与讨论页

参考：`设计图/即时消息任务联动页.png`

### 功能定位

消息页不是简单聊天室，而是“项目会话 + 任务讨论 + 系统通知”的统一沟通中心。

### 目标布局

三栏结构：

1. 会话列表
2. 当前消息线程
3. 右侧任务上下文面板

### 会话列表必须包含

- 项目群聊
- 任务讨论会话
- 系统通知会话 / 系统消息入口
- 未读数量
- 最近消息预览
- 置顶会话

### 消息区必须支持

- 文本消息
- 代码消息
- 任务引用消息
- 文件引用消息
- 回复提示 / Thread 关系
- 已读未读反馈（至少预留）
- @成员（可先做 UI + 结构，后续增强）

### 右侧上下文面板必须支持

- 当前任务状态
- 当前任务负责人
- 截止日期
- 优先级
- 小型活动时间线
- 快捷操作：更新进度 / 标记阻塞 / 跳转任务详情

### 当前实现基线

当前项目已实现：

- 项目消息列表
- WebSocket 项目实时消息
- 任务 thread_id 讨论
- 任务消息进入任务活动与通知

### 目标增强要求

后续实现必须向设计图靠齐：

- 不再只是单栏群聊
- 必须做成“会话列表 + 主消息区 + 上下文侧栏”结构
- 任务消息与任务详情联动必须清晰可见

---

## 8.6 文档协作页

参考：`设计图/文档协作页.png`

### 功能要求

- 文档列表页
- 新建文档
- 删除文档
- 查看文档版本
- 进入文档编辑器

### 目标增强要求

文档协作页最终要具备：

- 文档卡片/列表
- 文档作者、更新时间、版本数
- 文档权限状态
- 文档标签或关联任务提示
- 文档版本入口

### 文档编辑页要求

虽然设计图未单独给出编辑页全图，但功能必须完整：

- 标题可编辑
- Markdown / 富文本编辑（当前基线为 Markdown）
- 自动保存版本
- 显示版本历史
- 版本摘要
- 有效新增字数 / 修改字数记录

---

## 8.7 文件共享与资料评审页

参考：`设计图/文件共享资料评审页.png`

### 功能定位

文件页不只是上传下载，还要承担“资料汇总、任务附件、评审状态”的角色。

### 必须实现

- 文件列表
- 上传文件
- 删除文件
- 文件与任务关联
- 文件基础元数据：文件名、上传者、大小、类型、上传时间

### 目标增强要求

后续实现需要补到设计图级：

- 文件预览区
- 版本历史
- 评审状态（待评审 / 已通过 / 退回修改）
- 下载次数
- 关联任务
- 评论 / 评审意见
- 文件分类筛选与搜索

---

## 8.8 协作图谱页

参考：`设计图/协作图谱贡献分析页.png`

### 功能定位

展示 Git Graph 风格的协作事件链。

### 必须实现

- 基于 `CollaborationEvent` 的节点图
- 节点类型颜色区分：任务、文档、评论、文件、代码、完成、风险等
- 节点详情
- 分支线 / 关联边

### 数据来源

- `/api/projects/{id}/graph`

### 目标效果

- 类似 Git / 分支图谱
- 能看出谁做了什么、何时发生、与哪个任务/文档相关

---

## 8.9 贡献审计页

参考：`设计图/协作图谱贡献分析页.png`

### 功能定位

多维证据链贡献分析页。

### 必须展示

- 成员总分排行
- 任务得分
- 文档得分
- 代码得分
- 响应得分
- 稳定性得分
- 近期证据列表
- 完成任务数
- 协作事件数

### 必须实现

- 贡献分重算按钮
- 证据列表展示
- 按成员展开分析

### 数据来源

- `/api/projects/{id}/contribution`
- `/api/projects/{id}/contribution/recalculate`

---

## 8.10 风险雷达页

参考：`设计图/项目仪表盘页.png` 中风险模块 + `设计图/代码提交冲突风险页.png`

### 功能要求

- 风险列表
- 风险等级
- 风险分数
- 风险原因
- 风险建议
- 风险状态（open/resolved/ignored）
- 风险扫描按钮

### 风险类型至少包含

- 截止时间延期风险
- 任务阻塞风险
- 评审积压风险
- 长时间未更新风险
- 代码冲突高风险（目标增强）

---

## 8.11 Git 协作 / 代码提交页

参考：`设计图/代码提交冲突风险页.png`

### 当前基线功能

- 提交记录列表
- 记录 commit_message / branch_name / changed_files / added_lines / deleted_lines

### 目标增强要求

后续必须按设计图实现为“代码提交与冲突风险驾驶舱”，包括：

- 近期提交时间线
- 分支关系可视化
- 冲突风险文件列表
- 修改热点表
- 待处理合并请求（可先模拟数据结构，后续接真实仓库）
- 仓库绑定状态
- 成员、分支、风险等级筛选

### 数据模型基线

- `GitRepository`
- `GitCommit`

---

## 8.12 AI 项目经理页

参考：`设计图/AI助手页.png`

### 功能要求

- 输入项目名称
- 输入成员角色
- 输入截止时间
- 输入技术栈
- 调用 AI 生成任务规划
- 展示里程碑、任务列表、风险建议
- 一键确认写入项目
- 生成周报

### 当前后端基线

- `/api/ai/planning`
- `/api/ai/planning/{suggestion_id}/confirm`
- `/api/ai/reports/weekly`

### 实现要求

- 若 AI API 不可用，必须回退到规则模板生成
- 生成结果必须可审阅后再写入项目

---

## 8.13 项目设置与成员邀请页

参考：`设计图/项目设置成员邀请页.png`

### 功能定位

这是项目成员管理、项目基本配置、邀请闭环的核心页面。

### 必须实现

- 项目基础信息编辑
- 成员列表
- 角色调整
- 移除成员
- 邀请成员
- 邀请说明
- 待处理邀请列表
- 撤回邀请
- 角色权限矩阵

### 邀请流程必须完整

1. 组长搜索用户；
2. 选择角色；
3. 填写邀请说明；
4. 发起邀请；
5. 被邀请人在通知中心 / 项目列表接受或拒绝；
6. 接受后加入项目成员列表；
7. 项目仪表盘、通知中心、侧栏角标同步更新。

---

## 8.14 协作提醒中心页

参考：`设计图/协作提醒中心页.png`

### 功能定位

全局“我的工作台”。

### 必须包含

- 待处理邀请
- 未读提醒
- 我的待办
- 即将到期任务
- 提醒筛选：全部 / 未读 / @我 / 任务 / 系统
- 今日协作动态时间线

### 当前实现基线

- 侧边栏协作提醒抽屉
- 项目列表页工作台模块
- 通知 Store 与通知 API

### 目标增强要求

- 当前抽屉式提醒中心后续可以进一步升级为完整页面或更复杂抽屉；
- 但不论最终形式如何，业务能力必须完整实现。

---

## 9. 后端业务模型规范

以下模型是后续实现的核心数据基线：

### 9.1 用户域

- `User`
  - 用户名
  - 密码哈希
  - 昵称
  - 邮箱
  - 手机号
  - 头像
  - 系统角色
  - 状态

### 9.2 项目域

- `Project`
- `ProjectMember`
- `ProjectInvitation`

### 9.3 任务域

- `Task`
- `TaskDependency`
- `Milestone`
- `TaskActivity`

### 9.4 沟通域

- `Message`
- `Notification`

### 9.5 文档域

- `Document`
- `DocumentVersion`

### 9.6 协作审计域

- `CollaborationEvent`
- `ContributionScore`
- `ContributionEvidence`
- `RiskAlert`
- `PeerEvaluation`

### 9.7 文件与代码域

- `FileResource`
- `GitRepository`
- `GitCommit`

### 9.8 AI 域

- `AITaskSuggestion`
- `AIReportHistory`

---

## 10. 后端接口清单（必须保留并补全）

## 10.1 认证接口

- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `PUT /api/auth/me`
- `PUT /api/auth/me/password`
- `GET /api/auth/users/search`

## 10.2 项目接口

- `POST /api/projects`
- `GET /api/projects`
- `GET /api/projects/{project_id}`
- `PUT /api/projects/{project_id}`
- `DELETE /api/projects/{project_id}`
- `GET /api/projects/{project_id}/dashboard`
- `GET /api/projects/{project_id}/members`
- `PUT /api/projects/{project_id}/members/{user_id}`
- `DELETE /api/projects/{project_id}/members/{user_id}`

## 10.3 邀请接口

- `GET /api/projects/{project_id}/invitations`
- `POST /api/projects/{project_id}/invitations`
- `GET /api/me/invitations`
- `POST /api/invitations/{id}/accept`
- `POST /api/invitations/{id}/reject`
- `DELETE /api/invitations/{id}`

## 10.4 任务接口

- `GET /api/projects/{project_id}/tasks`
- `GET /api/me/tasks`
- `POST /api/projects/{project_id}/tasks`
- `GET /api/tasks/{task_id}`
- `PUT /api/tasks/{task_id}`
- `DELETE /api/tasks/{task_id}`
- `GET /api/tasks/{task_id}/activities`
- `GET /api/projects/{project_id}/milestones`
- `POST /api/projects/{project_id}/milestones`

## 10.5 消息接口

- `GET /api/projects/{project_id}/messages`
- `POST /api/projects/{project_id}/messages`
- `DELETE /api/messages/{message_id}`
- `WS /api/ws/{project_id}`

## 10.6 文档接口

- `GET /api/projects/{project_id}/documents`
- `POST /api/projects/{project_id}/documents`
- `GET /api/documents/{id}`
- `PUT /api/documents/{id}`
- `DELETE /api/documents/{id}`
- `GET /api/documents/{id}/versions`
- `POST /api/documents/{id}/versions`

## 10.7 文件接口

- `GET /api/projects/{project_id}/files`
- `POST /api/projects/{project_id}/files/upload`
- `DELETE /api/files/{id}`

## 10.8 审计与分析接口

- `GET /api/projects/{project_id}/graph`
- `GET /api/projects/{project_id}/contribution`
- `POST /api/projects/{project_id}/contribution/recalculate`
- `GET /api/projects/{project_id}/risk-alerts`
- `POST /api/projects/{project_id}/risk-alerts/scan`
- `PUT /api/risk-alerts/{id}/resolve`
- `GET /api/projects/{project_id}/git/commits`
- `POST /api/projects/{project_id}/git/commits`
- `GET /api/projects/{project_id}/peer-evaluations`
- `POST /api/projects/{project_id}/peer-evaluations`

## 10.9 通知接口

- `GET /api/notifications`
- `GET /api/notifications/unread-count`
- `POST /api/notifications/{id}/read`
- `POST /api/notifications/read`

## 10.10 AI 接口

- `POST /api/ai/planning`
- `POST /api/ai/planning/{suggestion_id}/confirm`
- `POST /api/ai/reports/weekly`

---

## 11. 前端状态管理与数据流规范

## 11.1 当前 Store 基线

- `useUserStore`
- `useProjectStore`
- `useNotificationStore`

## 11.2 必须遵守的数据流原则

1. 登录态统一由 `user store` 管理；
2. 当前项目、成员、仪表盘统一由 `project store` 管理；
3. 通知、邀请、我的待办统一由 `notification store` 管理；
4. 页面不要重复定义全局事实状态；
5. 列表页、详情页、抽屉、弹窗之间的数据要能刷新同步。

## 11.3 推荐增强

后续如项目规模扩大，可继续新增：

- `task store`
- `message store`
- `document store`
- `git store`

但前提是不要破坏当前简单清晰的数据组织方式。

---

## 12. 实时能力规范

## 12.1 当前 WebSocket 基线

当前已支持按项目维度的实时消息。

### 已有事件

- `message`
- `task_update`
- `user_online`
- `user_offline`
- `ping/pong`

## 12.2 后续目标

建议在不破坏兼容性的前提下，增强为更语义化事件：

- `message:new`
- `task:assigned`
- `task:status_changed`
- `task:progress_changed`
- `task:commented`
- `notification:new`
- `project:member_joined`

## 12.3 实现要求

- 页面中收到实时事件后必须局部更新 UI；
- 不允许所有操作都依赖整页刷新；
- 消息页、任务页、协作提醒中心必须优先支持实时体验。

---

## 13. 功能完整性要求

后续实现时，不能只把页面做出来，必须确保“功能闭环”。

### 13.1 成员协作闭环

必须满足：

1. A 注册并创建项目；
2. A 在项目设置页邀请 B；
3. B 在项目列表页或协作提醒中心看到邀请；
4. B 接受邀请；
5. 项目成员数变化；
6. B 可以进入该项目；
7. A 可以给 B 分配任务；
8. B 能收到通知并在任务页开始处理。

### 13.2 任务推进闭环

必须满足：

1. 创建任务；
2. 分配负责人；
3. 负责人接受并开始；
4. 更新进度；
5. 发表评论；
6. 进入待评审；
7. 标记完成；
8. 所有关键动作留下活动记录与协作事件。

### 13.3 消息联动闭环

必须满足：

1. 项目群聊可发消息；
2. 任务讨论可发消息；
3. 任务评论触发任务活动；
4. 相关成员收到通知；
5. 消息页能看到任务上下文。

### 13.4 文档闭环

必须满足：

1. 创建文档；
2. 进入编辑；
3. 保存版本；
4. 查看版本历史；
5. 删除文档；
6. 文档变化可作为协作证据被消费。

### 13.5 审计闭环

必须满足：

1. 有协作事件；
2. 图谱页能展示事件；
3. 贡献页能读取任务、文档、消息、代码等证据；
4. 风险页能显示可操作的风险建议。

---

## 14. 前端复刻要求（给 Codex 的明确执行规则）

后续按图实现前端时，必须遵守以下规则：

### 14.1 必须复刻的内容

1. 页面布局结构；
2. 侧边栏层级；
3. 卡片大小关系；
4. 标题、副标题、统计卡样式；
5. 表格、列表、时间线、进度条、标签、状态点；
6. 图表区域的占位与层级；
7. 右侧详情抽屉 / 详情栏的组织方式；
8. 顶部工具栏、筛选区、搜索区、按钮组。

### 14.2 不要求逐像素截图复制，但要求达到以下级别

- 打开页面后，信息结构与视觉观感应与设计图高度一致；
- 用户一眼能判断“这就是同一套设计系统”；
- 页面不允许出现粗糙、临时、默认管理后台风格；
- 新增页面必须像原本就属于这套 TeamFlow 产品。

### 14.3 必须实现的 UI 细节

- 空状态文案与插画/图标风格统一；
- Hover、按钮点击、抽屉展开、Tab 切换有基础过渡；
- 进度条、状态标签、未读角标、风险等级标签都必须有明确视觉差异；
- 卡片不可堆得拥挤或散乱，间距与层次必须整齐。

---

## 15. 已实现能力与后续补齐方向

## 15.1 当前已实现能力（可复用）

- 注册 / 登录
- 项目创建与列表
- 项目成员表与成员搜索
- 邀请成员与接受/拒绝邀请
- 仪表盘统计与部分图表
- 任务看板、任务详情、任务活动、任务评论
- 项目消息与 WebSocket 实时广播
- 协作文档与版本保存
- 文件上传与文件列表
- 协作图谱、贡献分、风险扫描
- Git 提交记录
- AI 项目规划与周报生成
- 通知中心 / 待办 / 协作提醒抽屉

## 15.2 后续必须优先补齐的方向

1. 会话式消息页完全对齐设计图；
2. Git 驾驶舱从“记录提交”升级为“冲突风险分析页”；
3. 文件共享页从“简单列表”升级为“资料评审页”；
4. 风险雷达页、过程审计页、里程碑页进一步完善；
5. 文档协作页视觉与信息层级对齐设计稿；
6. 协作提醒中心进一步增强成完整工作台体验。

---

## 16. 开发顺序建议

建议按以下顺序推进，保证每一阶段都可演示：

### 第一阶段：视觉统一

- 统一全局样式变量
- 统一左侧导航与顶部结构
- 将所有现有页面先拉齐到设计图基调

### 第二阶段：核心闭环页

- 项目列表页
- 项目仪表盘页
- 项目设置成员邀请页
- 协作提醒中心页
- 任务管理页
- 即时消息任务联动页

### 第三阶段：协作资产页

- 文档协作页
- 文档编辑页
- 文件共享资料评审页
- Git 协作页

### 第四阶段：分析审计页

- 协作图谱页
- 贡献审计页
- 风险雷达页
- 过程审计页（可新增）

### 第五阶段：智能能力页

- AI 项目经理页
- 自动周报 / 自动报告输出

---

## 17. 验收标准

只有满足以下标准，才算实现合格：

### 17.1 功能验收

- 所有主要路由页可访问；
- 所有核心接口可用；
- 邀请、任务、消息、文档、文件、通知都能形成闭环；
- 关键行为能写入事件、通知或活动记录；
- 不依赖手工修改数据库即可完成主流程演示。

### 17.2 视觉验收

- 页面整体风格统一；
- 侧边栏、卡片、标签、图表、抽屉与设计图同体系；
- 设计图已覆盖页面必须做到高相似度复刻；
- 新补页面不能“像原型”，必须“像可交付产品”。

### 17.3 联动验收

- 项目设置邀请成员后，项目列表 / 通知 / 协作提醒中心同步联动；
- 任务更新后，仪表盘 / 活动记录 / 通知同步联动；
- 文档和消息行为可沉淀为协作事件；
- 贡献分和风险扫描能消费真实过程数据。

### 17.4 演示验收

必须能够顺畅演示以下完整故事线：

1. 注册 2~3 个账号；
2. 创建项目并邀请成员；
3. 成员接受邀请；
4. 创建任务并分配；
5. 成员处理任务并讨论；
6. 上传文件、编辑文档；
7. 查看图谱、贡献分、风险预警；
8. 用 AI 生成规划或周报。

---

## 18. 给后续 Codex 的执行说明

1. 后续实现任何页面时，必须先查看对应设计图；
2. 实现前先判断当前页面与设计图差距，再做收敛，不可随意发挥新风格；
3. 所有新增组件优先复用当前全局变量、圆角、阴影、按钮规范；
4. 如果功能已在后端存在，优先接现有接口，不要重复造轮子；
5. 如果设计图展示了但当前后端未完全支持，就在当前模型和 API 基线之上补齐；
6. 所有页面实现必须兼顾：布局复刻、状态完整、交互闭环、真实数据联动；
7. 完成后必须进行构建、运行和主流程验证。

---

## 19. 结论

TeamFlow 的实现目标不是做一个普通的“管理后台”，而是做一个：

- 视觉上接近设计图、统一且高级；
- 功能上覆盖项目协作全过程；
- 数据上有证据链、有审计、有通知、有风险分析；
- 能用于课程设计展示、答辩和实际协作演示的完整产品。

因此，后续所有开发都必须围绕这三件事展开：

1. **严格遵守当前配色与设计图风格；**
2. **把所有核心协作闭环做完整；**
3. **让前端界面与后端功能一一对应，真实可用。**

