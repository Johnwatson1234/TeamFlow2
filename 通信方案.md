# TeamFlow 通信与协作闭环优化方案

## 0. 最高约束

- 现有 UI 已有的配色一律不改。
- 后续所有页面与交互优化，只能沿用当前系统既有的紫蓝主色、状态色、卡片/边框/阴影体系。
- 本方案聚焦“协作能力补全”，不做配色重构、不做视觉风格改版。

---

## 1. 现状判断

结合当前代码结构，系统已经具备以下基础能力：

- 已有注册、登录、用户搜索能力。
- 已有项目、项目成员、任务、消息、协作事件等核心表。
- 已有项目成员邀请入口，项目设置页可搜索用户并添加到项目。
- 已有项目群聊式消息页面，支持 WebSocket 实时广播。
- 已有任务看板，支持创建任务、修改状态、分配负责人。
- 已有协作事件、贡献分、风险扫描等审计基础设施。

但是它目前**还没有形成“多账号协作闭环”**，主要问题不在“完全没有基础”，而在于“能力分散、缺少串联”：

### 1.1 当前核心问题

1. **消息是项目广播，不是完整的协作通信模型**
   - 现在消息本质上是项目公共聊天。
   - 没有“会话”层，没有项目群聊 / 任务讨论 / 私聊的统一抽象。
   - 没有未读、已读、提醒、@提及、系统消息、任务关联消息。

2. **任务分配只是字段变化，不是完整的任务协作流程**
   - 虽然 `task.assignee_id` 已存在，但缺少：
   - 分配通知；
   - 接收方确认；
   - 任务评论区；
   - 任务状态流转记录；
   - “谁在什么时候把任务交给了谁”的审计日志。

3. **账号之间的关系依赖“同项目成员”，但缺少加入闭环**
   - 当前更接近“组长直接把人加进项目”。
   - 缺少“邀请待处理 / 接受 / 拒绝 / 过期”机制。
   - 被邀请者缺少统一入口查看自己收到的协作邀请。

4. **任务追踪有看板，但没有事件链**
   - 现在能看状态和进度，但缺少完整追踪视角：
   - 创建 → 分配 → 开始处理 → 阻塞 → 评论沟通 → 完成 → 验收。

5. **消息、任务、协作审计三套能力没有真正打通**
   - 消息发送后没有沉淀为“协作上下文”。
   - 任务变化没有自然反馈到消息流和通知流。
   - 仪表盘看得到结果，但看不到“沟通驱动任务推进”的全过程。

---

## 2. 目标定义

本次优化的目标，不是单独增强聊天，而是把系统升级为：

> **“以项目为中心、以任务为主线、以消息为触发、以协作事件为审计依据”的多账号协作系统。**

### 2.1 需要最终解决的 4 个问题

1. 不同账号可以在同一项目内稳定协作；
2. 一个账号可以把任务明确分配给另一个账号；
3. 消息可以和任务、提醒、协作事件关联；
4. 任务推进过程可以被实时传递、实时查看、事后追踪。

---

## 3. 建议的总体设计

建议采用**“项目协作优先，私信次之”**的建设顺序。

### 3.1 为什么不建议先做全局私信

因为当前系统核心业务是课程设计小组协作，不是社交聊天。  
现阶段最应该先打通的是：

- 用户 A 创建项目；
- 邀请用户 B、C 加入；
- A 给 B 分配任务；
- B 收到提醒并更新进度；
- C 在任务下评论；
- 所有人都能看到任务与沟通的全过程；
- 仪表盘、贡献分、风险雷达可消费这些过程数据。

所以建议优先做：

1. 项目成员闭环；
2. 项目群聊；
3. 任务讨论；
4. 任务通知；
5. 协作追踪；

然后再扩展个人私信。

---

## 4. 目标业务流程（推荐）

### 4.1 项目协作主流程

1. 用户注册 / 登录；
2. 用户 A 创建项目；
3. 用户 A 邀请用户 B、C 加入项目；
4. B、C 在“我的邀请”中接受邀请；
5. 项目自动生成默认群聊；
6. A 在任务看板创建任务并分配给 B；
7. 系统给 B 发送：
   - 站内通知；
   - 任务系统消息；
   - 实时 WebSocket 推送；
8. B 进入任务详情，开始处理并更新进度；
9. B / C 可在该任务下发消息、评论、@成员；
10. 每次状态变化都写入协作事件；
11. 仪表盘、贡献分、风险分析直接消费这些事件和任务数据。

### 4.2 用户视角的功能闭环

#### 组长视角

- 邀请成员；
- 分配任务；
- 看谁未接受邀请；
- 看谁未响应任务；
- 看任务阻塞原因；
- 看项目整体协作推进情况。

#### 成员视角

- 查看收到的项目邀请；
- 查看“分配给我的任务”；
- 收到被@、被分配、被催办、被回复的提醒；
- 在任务上下文里沟通，不需要来回切页面找信息。

#### 教师 / 审计视角

- 查看成员加入时间；
- 查看任务流转链路；
- 查看谁分配、谁接收、谁推进、谁完成；
- 查看消息与任务、事件之间的证据关联。

---

## 5. 核心能力设计

## 5.1 协作模型分层

建议将协作能力拆成 4 层：

### 第一层：成员关系层

负责解决“哪些账号能协作”的问题。

- 项目
- 项目成员
- 项目邀请
- 成员角色

### 第二层：会话通信层

负责解决“账号之间在哪里沟通”的问题。

- 项目群聊
- 任务讨论
- 系统通知会话
- 后续可扩展私聊

### 第三层：任务流转层

负责解决“任务如何从人 A 流向人 B”的问题。

- 创建
- 分配
- 接收
- 开始
- 阻塞
- 评论
- 完成
- 验收

### 第四层：协作审计层

负责解决“如何追踪、统计、分析”的问题。

- 协作事件
- 通知记录
- 任务流转记录
- 已读/未读
- 贡献证据

---

## 6. 数据模型改造建议

当前表结构已经有不错基础，建议在此之上做增量扩展。

## 6.1 保留并继续使用的现有表

- `user`
- `project`
- `project_member`
- `task`
- `message`
- `collaboration_event`

这些不需要推翻重来，重点是补齐中间能力。

## 6.2 新增表建议

### 6.2.1 `project_invitation`

用于解决“邀请闭环”。

建议字段：

- `id`
- `project_id`
- `inviter_id`
- `invitee_id`
- `role`
- `status`：`pending / accepted / rejected / expired / revoked`
- `message`
- `created_at`
- `responded_at`

用途：

- 组长发邀请；
- 被邀请者在个人入口处理邀请；
- 接受后再写入 `project_member`。

> 一期如果想更快落地，也可以先保留“直接加入项目”的逻辑，同时补一个通知记录；二期再补正式邀请单。

### 6.2.2 `conversation`

用于抽象“消息应该发到哪里”。

建议字段：

- `id`
- `project_id`
- `conversation_type`：`project_group / task / direct / system`
- `related_task_id`
- `name`
- `created_by`
- `created_at`

用途：

- 一个项目默认一个 `project_group`；
- 每个任务可有一个 `task` 会话；
- 后续可扩展成员私聊。

### 6.2.3 `conversation_member`

用于解决会话成员和未读状态。

建议字段：

- `id`
- `conversation_id`
- `user_id`
- `last_read_message_id`
- `last_read_at`
- `mute_flag`
- `joined_at`

用途：

- 记录谁在这个会话里；
- 统计每个人未读数；
- 支持消息角标。

### 6.2.4 `notification`

用于统一提醒中心。

建议字段：

- `id`
- `user_id`
- `project_id`
- `type`：`project_invite / task_assigned / task_mentioned / task_due / message_mentioned / task_status_changed`
- `title`
- `content`
- `ref_type`
- `ref_id`
- `is_read`
- `created_at`
- `read_at`

用途：

- 个人首页提醒；
- 侧边栏红点；
- 任务分配通知。

### 6.2.5 `task_activity`

用于完整记录任务流转轨迹。

建议字段：

- `id`
- `task_id`
- `project_id`
- `actor_id`
- `activity_type`：`created / assigned / accepted / commented / started / progress_updated / blocked / reopened / completed / reviewed`
- `from_value`
- `to_value`
- `content`
- `created_at`

用途：

- 任务时间线；
- 谁改了什么一目了然；
- 给仪表盘和贡献分析提供结构化证据。

## 6.3 对现有 `message` 表的建议调整

建议新增或调整字段：

- `conversation_id`：替代单纯依赖 `project_id + thread_id`
- `reply_to_message_id`
- `mentioned_user_ids`（JSON 或关联表）
- `client_msg_id`（避免前端重复发送）
- `status`：`sent / delivered / read / recalled`

现有 `thread_id` 可以保留一段时间做兼容，但长期建议弱化。

---

## 7. 后端接口设计建议

## 7.1 邀请与成员接口

### 建议新增

- `GET /api/me/invitations`
- `POST /api/projects/{project_id}/invitations`
- `POST /api/invitations/{invitation_id}/accept`
- `POST /api/invitations/{invitation_id}/reject`
- `POST /api/projects/{project_id}/members/{user_id}/role`

### 说明

- 现在 `inviteMember` 更像“直接加成员”；
- 优化后应区分：
  - 发出邀请；
  - 接受邀请；
  - 成为成员。

## 7.2 会话与消息接口

### 建议新增

- `GET /api/projects/{project_id}/conversations`
- `POST /api/projects/{project_id}/conversations`
- `GET /api/conversations/{conversation_id}/messages`
- `POST /api/conversations/{conversation_id}/messages`
- `POST /api/conversations/{conversation_id}/read`

### 说明

- `project_group`：项目主群；
- `task`：任务讨论串；
- `system`：系统通知流；
- 后续可扩展 `direct`：成员私聊。

## 7.3 任务协作接口

### 建议新增或调整

- `POST /api/tasks/{task_id}/assign`
- `POST /api/tasks/{task_id}/accept`
- `POST /api/tasks/{task_id}/start`
- `POST /api/tasks/{task_id}/block`
- `POST /api/tasks/{task_id}/complete`
- `GET /api/tasks/{task_id}/activities`
- `GET /api/tasks/{task_id}/conversation`

### 说明

- 不建议所有语义都塞进通用 `update task`；
- 关键动作应有独立接口，便于：
  - 写审计日志；
  - 发通知；
  - 做权限控制；
  - 后续统计分析。

## 7.4 通知接口

### 建议新增

- `GET /api/notifications`
- `POST /api/notifications/read`
- `POST /api/notifications/{id}/read`
- `GET /api/notifications/unread-count`

---

## 8. WebSocket 设计建议

当前 WebSocket 已有基础，但事件类型过少，建议升级为统一事件总线。

## 8.1 推荐事件类型

- `message:new`
- `message:updated`
- `message:deleted`
- `conversation:read`
- `notification:new`
- `task:assigned`
- `task:status_changed`
- `task:progress_changed`
- `task:commented`
- `project:member_joined`
- `project:member_left`

## 8.2 推荐推送策略

### 项目频道

推送给项目内所有在线成员：

- 项目群聊消息；
- 成员加入/退出；
- 任务状态变化；
- 仪表盘相关刷新事件。

### 用户频道

推送给特定用户：

- 邀请通知；
- 被分配任务；
- 被@提醒；
- 私聊消息；
- 待办提醒。

> 建议从“仅项目频道”升级为“项目频道 + 用户频道”双通道模型。

---

## 9. 前端改造建议

重点是增强协作，不改配色风格。

## 9.1 项目设置页

当前已具备成员搜索与添加雏形，建议增强为：

- 成员列表显示角色、加入时间、在线状态；
- 邀请状态显示：待处理 / 已接受 / 已拒绝；
- 增加“复制邀请链接 / 重新发送 / 撤回邀请”；
- 仍沿用当前卡片、边框、按钮配色体系。

## 9.2 消息页

当前消息页建议从“单一群聊”升级为“两栏布局”：

### 左侧：会话列表

- 项目群聊
- 我参与的任务讨论
- 系统通知
- 后续可扩展私聊

### 右侧：消息内容区

- 支持文本、代码、任务引用；
- 支持 @成员；
- 支持点击任务跳转；
- 显示未读数和最近消息时间。

> 配色不改，只在现有消息页结构上增加信息层次。

## 9.3 任务看板页

建议增强为：

- 卡片上显示负责人响应状态：未接收 / 处理中 / 阻塞 / 待验收；
- 任务详情抽屉增加“讨论”与“活动”两个分区；
- 分配任务时自动发送系统消息；
- 任务更新后同步刷新消息与通知角标。

## 9.4 仪表盘

建议新增以下可视化块：

- 待接受邀请数；
- 待响应任务数；
- 任务阻塞数；
- 未读消息数；
- 最近任务活动时间线。

## 9.5 全局个人入口

建议新增一个轻量级“我的待办 / 我的通知”入口：

- 我收到的邀请；
- 分配给我的任务；
- @我的消息；
- 即将到期任务。

这个入口非常关键，它能让“不同账号之间的传递”真正被用户感知到。

---

## 10. 与现有代码的衔接方式

为了减少推翻式重构，建议在当前目录结构上演进：

### 10.1 后端改造落点

- `backend/app/models/project.py`
  - 增加邀请模型，必要时补成员状态字段语义。

- `backend/app/models/message.py`
  - 从“项目消息表”升级为“会话消息表”。

- `backend/app/models/task.py`
  - 增加任务活动表，或扩展任务流转记录。

- `backend/app/models/collaboration.py`
  - 继续作为审计事件中心使用，不建议废弃。

- `backend/app/api/projects.py`
  - 增加邀请、成员状态、成员角色调整接口。

- `backend/app/api/messages.py`
  - 从项目消息列表升级为会话消息列表；
  - WebSocket 增加更多事件类型。

- `backend/app/api/tasks.py`
  - 增加任务分配、接收、阻塞、完成、活动时间线接口。

- 可新增：
  - `backend/app/api/notifications.py`
  - `backend/app/api/invitations.py`
  - `backend/app/api/conversations.py`

### 10.2 前端改造落点

- `frontend/src/views/project/SettingsView.vue`
  - 升级成员邀请与成员状态管理。

- `frontend/src/views/project/MessagesView.vue`
  - 升级为会话式消息页面。

- `frontend/src/views/project/TasksView.vue`
  - 增加任务讨论与活动追踪。

- `frontend/src/views/project/DashboardView.vue`
  - 加入待办、提醒、最近任务活动统计。

- `frontend/src/layouts/ProjectLayout.vue`
  - 只增加角标、通知入口，不改主配色。

- `frontend/src/api/index.ts`
  - 新增 invitations / conversations / notifications / task actions API。

---

## 11. 推荐实施顺序

建议分三期做，降低风险。

## 一期：先打通“同项目多账号协作”

目标：让两个账号真正能协作起来。

### 范围

- 邀请成员闭环（至少做到邀请通知 + 成员加入可见）；
- 项目群聊稳定可用；
- 任务可明确分配给项目成员；
- 被分配者收到通知；
- 任务状态变化写入协作事件；
- 仪表盘显示最近任务活动。

### 一期完成后的结果

- A 可以邀请 B；
- B 登录后能看到项目；
- A 可以给 B 分配任务；
- B 能收到提醒并更新任务；
- A 能看到 B 的推进情况。

## 二期：补齐“消息—任务—通知”联动

### 范围

- 任务讨论会话；
- @提及；
- 未读数；
- 任务详情活动时间线；
- 系统消息自动生成。

### 二期完成后的结果

- 每个任务都有自己的讨论上下文；
- 用户不会错过别人点名、分配、催办、回复。

## 三期：补齐“个人工作台 + 更强审计”

### 范围

- 我的通知；
- 我的待办；
- 我的邀请；
- 更细粒度的协作证据；
- 后续视情况扩展私聊。

### 三期完成后的结果

- 用户不需要进入每个项目逐个找消息；
- 教师 / 组长能更清楚看到协作质量和执行质量。

---

## 12. 权限与规则建议

## 12.1 成员规则

- 只有项目成员才能看到项目内消息和任务；
- 非成员不能连接项目 WebSocket；
- 被移出项目后自动失去会话访问权。

## 12.2 角色规则

### 组长

- 邀请成员；
- 分配任务；
- 调整成员角色；
- 查看所有协作数据。

### 成员

- 处理分配给自己的任务；
- 参与项目群聊；
- 参与相关任务讨论。

### 教师

- 可旁观或审计；
- 是否允许直接分配任务，可作为配置项。

## 12.3 审计规则

以下行为必须写入 `collaboration_event` 或 `task_activity`：

- 邀请成员；
- 成员接受邀请；
- 任务分配；
- 任务状态变更；
- 阻塞申报；
- 完成提交；
- 关键消息（如 @、任务引用、系统通知）。

---

## 13. 验收标准（非常重要）

如果下面 8 条都成立，就说明这次优化是成功的：

1. 两个不同账号可以加入同一项目；
2. 项目内成员都能看到同一项目群聊；
3. 组长可以把任务分配给指定成员；
4. 被分配成员能收到明确提醒；
5. 成员更新任务进度后，其他成员能实时看到；
6. 每个任务都有可追踪的活动记录；
7. 仪表盘能展示最近协作推进情况；
8. 全过程不改现有 UI 配色风格。

---

## 14. 我的建议结论

### 结论一句话

**不要把问题理解成“再加一个聊天功能”，而是要把系统补成“项目成员 + 会话 + 任务流转 + 通知 + 审计”五位一体的协作闭环。**

### 最推荐的落地路线

优先顺序建议如下：

1. 先补项目邀请闭环；
2. 再补任务分配通知；
3. 再补任务讨论与活动时间线；
4. 最后补个人通知中心和未读体系。

这样做的好处是：

- 改动方向清晰；
- 与现有代码兼容度高；
- 能最快让“不同账号之间协作”真正跑起来；
- 后续贡献分析、风险预警、AI 项目经理都会得到更可靠的过程数据。

---

## 15. 补充说明：当前系统并不是完全不能做，而是“缺闭环”

从代码结构看，系统已经有：

- 成员搜索；
- 项目成员；
- 消息；
- 任务负责人；
- WebSocket；
- 协作事件；

所以这不是从 0 开始推翻重做。  
更准确地说，当前系统已经有“协作零件”，下一步要做的是把这些零件串成一个完整工作流。

---

## 16. 后续开发建议

如果按这个方案继续推进，下一步最适合直接进入开发的一期任务可以拆成：

1. 新增邀请与通知数据结构；
2. 改造项目成员加入流程；
3. 为任务分配增加通知与协作事件；
4. 为任务详情增加活动时间线；
5. 为消息页增加会话抽象和未读计数。

如果需要，下一步我可以继续把这份方案细化成：

- 数据库表设计清单；
- 后端 API 清单；
- 前端页面改造清单；
- 一期开发任务拆分表。
